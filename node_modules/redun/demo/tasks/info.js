module.exports = {
  formula: {
    '#deps': '?simple',
    'processId': process.pid,
    'platform': process.platform
  },

  action: function(callback) {
    console.log('Task Name:', this.name);
    console.log('Task Config:', this.config);
    console.log('Task Dependencies:', this.deps);

    callback();
  }
};
