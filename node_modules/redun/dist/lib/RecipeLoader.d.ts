import { Gulp } from 'gulp';
export interface RecipeInfo {
    path: string;
    name: string;
    nameOverride: string | Function;
    originQualifier?: string;
}
export declare class RecipeLoader {
    static registerTasks(gulp: Gulp): void;
    private static taskFamilies;
    private static logTaskFamily(family);
    private static clearOptionalDependencies(tasks);
    private static registerTasksWithGulp(taskFamily, gulp);
    constructor(info: RecipeInfo);
    load(): void;
    private loadRecipe();
    private parse(recipe);
    private finalName();
    private recipeInfo;
}
