export declare class Recipe {
    name: string;
    formula: any;
    action: Function;
    children: Recipe[];
    constructor(name: string, formula: string | string[] | {
        [key: string]: string;
    }, action: Function, parent?: Recipe);
    getDependencies(familyOnly?: boolean): string[];
    private applyInheritedProperties(parent);
    private mergeParentProperties();
    private moveChildren(action);
    private moveMetaProperties();
    private recordNonInheritableProperties();
    private renamePlusPrefixedProperties();
    private meta;
    private nonInheritable;
}
